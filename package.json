{
  "name": "google-maps-draw",
  "version": "0.0.0",
  "description": "Fully featured drawing toolbox control for Google Maps API",
  "main": "dist/google-maps-draw.js",
  "scripts": {
    "lint": "jshint lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run lint && watchify lib/index.js --debug -s google-maps-draw -t babelify --outfile dist/google-maps-draw.js",
    "dist": "npm run lint && browserify lib/index.js -s google-maps-draw -t babelify --outfile dist/google-maps-draw.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/AppGeo/google-maps-draw.git"
  },
  "keywords": [
    "google",
    "maps",
    "draw",
    "control",
    "tools",
    "measurement",
    "area",
    "selection"
  ],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/AppGeo/google-maps-draw/issues"
  },
  "homepage": "https://github.com/AppGeo/google-maps-draw",
  "devDependencies": {
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "jshint": "^2.6.3",
    "watchify": "^2.4.0"
  }
}
